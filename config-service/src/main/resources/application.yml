spring:
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/services
  profiles:
    active: native
  security:
    user:
      password: admin
      name: user

server:
  port: 8001

#management:
#  endpoints:
#    web:
#      exposure:
#        include: health, info